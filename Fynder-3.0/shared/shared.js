/*
 * shared.js — Referencia de funciones globales de Fynder
 *
 * Todas estas funciones están implementadas en shared/fynder.js
 * Este archivo es solo documentación.
 *
 * NAVEGACIÓN
 *   goPage(p)              Navega a la página con id="page-{p}"
 *   goBack()               Vuelve a la página anterior
 *
 * MODAL DE NEGOCIO
 *   openModal(id)          Abre el modal de detalle del negocio
 *   closeModalDirect()     Cierra el modal
 *
 * FAVORITOS
 *   toggleFav(id)          Alterna favorito de un negocio
 *
 * NOTIFICACIONES
 *   showToast(msg, type)   Muestra un toast (type: 'success'|'error')
 *
 * TARJETAS
 *   gridCardHTML(b)        HTML de tarjeta en modo grid
 *   listCardHTML(b)        HTML de tarjeta en modo lista
 *
 * TEMA
 *   toggleTheme()          Alterna modo claro/oscuro
 *
 * AUTENTICACIÓN
 *   loginUser(event)       Maneja el login (en paginas/login/login.js)
 *   registerUser(event)    Maneja el registro (en paginas/registro/registro.js)
 *   logoutUser()           Cierra sesión
 */
